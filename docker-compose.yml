version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    restart: always
